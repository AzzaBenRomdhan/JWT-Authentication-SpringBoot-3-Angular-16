spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authentication
    username: postgres
    password: Admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 465
    username: celina.celine999@gmail.com
    password: pcnpyxvwembigxwb
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9090/realms/authentication"
application:
server:
  port: 8088